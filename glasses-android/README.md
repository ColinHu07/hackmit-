# Kith · Android perception

Reserved for Milestone 4. No Android app or SDK dependency is installed yet.

The verified target is Meta Wearables Device Access Toolkit **0.9.0**. Use the documented `DeviceSession.addCamera(StreamConfiguration)` camera lifecycle when implementing this milestone. See [capability verification](../docs/meta-capabilities.md) for sources and device-test gates.

Camera streaming must work before MediaPipe, calibration, or pet interactions are introduced. Display Web App plus native DAT camera concurrency is `NEEDS_DEVICE_TEST`.

# Multiplayer works without credentials. Photo verification is optional.
# Keep this server-side only; never expose it through Vite or a phone client.
MODEL_API_KEY=
# Meta Model API defaults shown here are used when these values are blank.
META_MODEL_API_BASE_URL=https://api.meta.ai/v1
META_MODEL_ID=muse-spark-1.3
